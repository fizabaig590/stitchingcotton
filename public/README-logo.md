# Logo (public)

Place your logo file here so the app can load it from Vercel without hardcoding local disk paths.

Expected file:
- `public/image.png`

Update:
- Replace `public/image.png` with your new logo (same filename).
- If you change the filename, also update `src/main.jsx` to match.

